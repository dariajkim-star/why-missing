# THESIS — 재무공시 누락·비표준 보고 스크리너

> ## 🔺 현재 상태 배너 (2026-08-03 기준 — 읽기 전에 이것부터)
>
> **이 문서는 2026-07-22~07-31에 쓰인 기록물이다. §9 마감 선언은 2026-07-31자이며, 그 이후
> 2026-08-03에 여러 판정이 더 있었다. 그 판정들은 §10에 있다.**
>
> **한 줄로**: 이 문서의 **밸류에이션 축 문언은 전부 폐기됐다.** 2026-08-03 G1 피해 크기 측정이
> 미달(V2 발동)로 확정되면서, 이 프로젝트에 **밸류에이션 축의 살아 있는 격차가 없다**는 것이
> 관측으로 닫혔다(`docs/g1-damage-measurement-results.md` §10). 사전등록 귀결표는 그 경우
> **"밸류에이션·투자 신호 문언 전면 폐기가 전제"**라고 측정 전에 못박아 두었다
> (`docs/g1-damage-measurement-preregistration.md` §6-1 3행).
>
> | 절 | 상태 |
> |---|---|
> | §1 BLUF · 정본 문제정의의 **최종 질문** | ⚠️ **주어를 잃음** — 폐기하지 않고 각주로 daria 판단 요청(§1 각주) |
> | §3 부품 매핑 표 — `opacity_rank` · 배제 판정 · **되살린 밸류 델타** 행 | 🔴 **폐기**(§3 폐기 표시) |
> | §5 예시 문장 *"EV/EBITDA가 6.2에서 9.8로"* | 🔴 **폐기**(§5 폐기 표시) |
> | §6 "지금까지 확정된 출력 설계" ①`opacity_rank` ②배제리스트 | 🔴 **폐기 — 제목의 "지금까지 확정된"은 2026-07-22~29 시점의 현재형이다**(§6 폐기 표시) |
> | §9 마감 선언(07-31) | ✅ **취소가 아니라 갱신** — §10이 이어받는다 |
> | §2 소급 재작성 금지 | ✅ **이 문서 자신에게도 적용된다.** 아래 폐기 표시는 전부 원문을 남긴 채 붙인 것이며, 지운 문장은 하나도 없다 |
>
> 🚫 **이 문서에서 인용 금지인 것들**은 §10-3에 목록으로 박혀 있다. 이 문서의 어느 숫자든
> 옮기기 전에 §10-3을 먼저 읽을 것.

> **BLUF**: 기업 공시에서 특정 재무 개념이 **경제적으로 존재하는데도 표준 XBRL 금액이 잡히지
> 않는** 회사를 찾아, **①진짜 미공시·누락 ②다른 곳에 적힌 것 ③애초에 0**으로 구분한다.
> 최종 질문은 — **정상화하면 기업가치·재무비율·투자자 판단이 실제로 달라지는가.**
> `crm-targeting-lab`은 이 스크리너를 짓기 위한 **방법론 리허설**이었다 — 도메인(고객 이탈)은
> 대리였고, 척추(모델이 아니라 프레임 · 불확실을 확실로 세탁하지 않는다)는 처음부터 이것을 향했다.

---

## 1. 정본 문제정의 (복명복창 대상 — 파티 소환 시마다)

> **기업 공시에서 특정 재무 개념이 경제적으로 존재하는데도 표준 XBRL 금액이 잡히지 않는 회사를
> 찾아서, ①진짜 미공시·누락인지 ②다른 곳에 적힌 것인지 ③애초에 0인 것인지를 구분하는
> 재무공시 누락·비표준 보고 스크리너를 만든다.**
>
> **최종 질문**: 누락되거나 비표준적으로 보고된 금액을 **정상화하면 기업가치·재무비율·투자자
> 판단이 실제로 달라지는가?**

이 프로젝트의 모든 결정은 이 문단으로 되돌아가 검산된다. **파티를 소환할 때마다 복명복창하고
시작한다** — daria 지시(2026-07-22 최초, 2026-07-29 정본 갱신).

> **⚠️ 각주 (2026-08-03) — 최종 질문의 주어에 대하여. 이 각주는 정본을 고치지 않는다.**
>
> 위 정본 문제정의의 **최종 질문**("정상화하면 기업가치·재무비율·투자자 판단이 실제로
> 달라지는가")에 대해, 레아 도메인 판정은 다음과 같이 적었다 —
> **"최종 질문은 지금 답할 수 없고, 현 표적 아래에서는 애초에 잘못 세워진 질문(ill-posed)이다.
> 정상화할 대상(총액 밖에 있는 실재 금액)이 74곳 검증에서 한 번도 관측되지 않았다."**
> (`docs/rescope-2026-08-03-leah-domain-verdict.md` §0-3 · §3, C1 🔴 0관측)
> 같은 문서 §4는 **"최종 질문의 현 문언 — 현 표적 아래에서는 죽는다"**로 판정했다.
> 즉 질문이 틀렸다는 것이 아니라 **주어(정상화 대상)가 관측되지 않았다**는 것이다.
>
> **BLUF와 정본의 이 문언을 나는 고치지 않았다.** 정본 문제정의의 개정은 **daria의 권한**이며
> (§1 본문: daria 지시로만 갱신), 문서 담당이 소급해 손대면 그것이 §2가 금지한 역사 왜곡이다.
>
> 🔵 **daria에게 요청하는 판단 (미결)**: 최종 질문을 ①현 문언 그대로 두고 "주어 없음"을 각주로만
> 남길 것인가, ②레아 §3-1이 적은 **표적 교체**(입구 조건을 "표준 태그 부재"에서 "총액 밖임이
> 원문에서 확인된 것"으로)를 전제로 다시 세울 것인가, ③레아 §3-2가 제시한 강등된 질문
> (*"이 격차를 보정하면 스크린에 들어오고 나가는 회사가 달라지는가"* — 밸류에이션 질문에서
> **데이터 품질 질문으로의 강등**임을 명기하는 조건부)로 대체할 것인가.
> **파티는 이 셋 중 어느 것도 스스로 고르지 않는다.** 결정 전까지 복명복창은 현 문언 그대로 한다.

### 1-1. 원 painpoint (2026-07-22, 역사 보존)

> *"재무제표에 의도적으로 누락된 밸류에이션 관련 지표 때문에 기업 가치가 왜곡된다. 나는 이걸
> 워싱으로 보고, 투자 대상 선정에서 그런 기업을 걸러내고 싶다."*

**폐기가 아니라 정밀화다.** 위 정본이 이를 대체한다. 바뀐 것 셋:

1. **"워싱"이라는 유죄 함의가 빠졌다** — 레아 원칙("고의를 판정하지 말고 격차를 드러내라")이
   문제정의 **자체에 내장**됐다. 산출물은 "이 회사는 나쁘다"가 아니라 "이 숫자는 표준 위치에 없다".
2. **①②③ 3분류가 걸림돌이 아니라 산출물 그 자체다.** 스파이크 1~2에서 ②③이 잔차의 대부분을
   차지한 것은 실패가 아니라 이 스크리너가 하기로 한 일이다.
3. **최종 질문이 셋이다** — 기업가치 · **재무비율** · 투자자 판단. 스파이크 1의 K3는
   기업가치(되살림÷시총) 하나만 쟀다.

## 2. 이 문서가 선언하는 것 (소급 재작성이 아니라 선언)

`crm-targeting-lab`의 표면 주제였던 *"누구에게 얼마를 써서 붙잡을 것인가"*는 **대리 문제**였다.
BankChurners 데이터가 이탈을 보여줬고, 이탈이 보이니 "붙잡자"가 나왔다 — 데이터가 시킨 주제지
사람이 아프다고 한 주제가 아니었다.

**리허설 문서를 소급해서 고치지 않는다.** 그것은 역사 왜곡이다. 대신 이 문서가 **through-line을
선언**한다: crm에서 만든 것이 무엇의 예행이었는지, 무엇이 본공연으로 옮겨지고 무엇이 버려지는지.

## 3. 부품 매핑 — 리허설에서 본공연으로

방법론 척추는 도메인만 갈아끼우면 그대로 선다.

| crm-targeting-lab (리허설) | 워싱 스크리너 (본공연) | 계승되는 계약 |
|---|---|---|
| `churn_score` (순위 전용, OOF) | **`opacity_rank`** — 동종업계 대비 공시 격차의 크기·개수, **순위 전용** | AD-5 투트랙: 순위축과 크기축을 한 컬럼에 섞지 않는다 |
| `churn_prob_calibrated` (확률 전용) | 되살린 밸류에이션의 **확률/신뢰 축** (설계 예정) | 확률과 순위를 교차 사용 금지 |
| `quadrant_official` (공식 판정, 단일 소유) | **배제 판정** — "이 공시 수준으론 밸류를 신뢰 못 함 → 패스", 규칙 하나가 단일 소유 | AD-12: 판정 규칙 단일 소유, 소비자는 자체 컷 금지 |
| `expected_saving` (되살린 금액) | **되살린 밸류 델타** — 누락분을 보수적 가정으로 복원 시 밸류에이션 이동 폭 | AD-9: 산식 단일 소유, 소비자는 재구현 금지 |
| 음수 절감액 = "접촉하지 말 것"도 정당한 답 | 배제 = "이 가격에 이 불투명도는 안 산다"도 정당한 답 | 판정을 세탁하지 않고 격차를 드러낸다 |
| D1 구조: **순위는 전원에게, 선택은 별도 규칙** | 전 종목에 `opacity_rank`, **배제는 별도 컷** | 순위 부여와 배제 결정의 분리 |
| 3-4 민감도: "가정이 틀리면 결론이 어디서 뒤집히나" | **배제 반전 구간**: "가정 조금 바꾸면 통과되는 거 아니냐"에 표로 답 | AD-4: 가정 단일 출처 + 그리드 스윕 |
| 가정 라벨링 (NFR1) | 복원 가정(보수/중앙/낙관)을 라벨링해 노출 | 실측과 가정을 문서에서 분리 |

> **🔴 폐기 표시 (2026-08-03) — 위 표의 원문은 그대로 둔다(§2: 소급 재작성 금지).**
> 아래 세 행은 **본공연 측 항목이 폐기**된 것이며, 리허설 측(crm) 원본과 "계승되는 계약"
> 열은 그대로 유효하다. 형식은 §8 전례(Mary 조항 — 조용히 안 쓰는 것은 부채이므로 명시한다)를 따른다.
>
> | 표의 행 | 처분 |
> |---|---|
> | ~~`opacity_rank` — 동종업계 대비 공시 격차의 크기·개수, 순위 전용~~ | **폐기(2026-08-03).** 사유: 전제(총액 밖 복원 대상의 존재)가 **C1 0관측**. 레아 §2 "후보에서 뺀 것"이 *"불투명성 순위 — 뺐다. 전제가 0관측이다"*로, §4가 **"죽음 확정 — 재확인"**으로 판정. 근거: `docs/rescope-2026-08-03-leah-domain-verdict.md` §2·§4 |
> | ~~**배제 판정** — "이 공시 수준으론 밸류를 신뢰 못 함 → 패스"~~ | **폐기(2026-08-03).** 같은 사유. 이미 2026-07-31 마감에서 미구현 처분됐고(§8·§9), 이 날짜에 **회계적 사망 진단서**가 붙었다 — *"항등식상 잔액 개념에서 복원 대상은 부외에만 살고, 부외는 개별 검증에서 0이었다"*. 근거: 같은 문서 §4 |
> | ~~**되살린 밸류 델타** — 누락분을 보수적 가정으로 복원 시 밸류에이션 이동 폭~~ | **폐기(2026-08-03).** 밸류에이션 축 문언의 핵심. G1 피해 측정 미달(V2)로 **밸류에이션 축에 살아 있는 격차가 없음**이 확정되었고, 사전등록 귀결표가 이 경우 **"밸류에이션·투자 신호 문언 전면 폐기가 전제"**라고 측정 전에 정해 두었다. 근거: `docs/g1-damage-measurement-results.md` §6·§10, `docs/g1-damage-measurement-preregistration.md` §6-1 |
>
> 파생 행도 함께 죽는다: ~~"배제 = 이 가격에 이 불투명도는 안 산다"~~ · ~~"전 종목에 `opacity_rank`,
> 배제는 별도 컷"~~ · ~~"배제 반전 구간"~~ · ~~"복원 가정(보수/중앙/낙관) 라벨링"~~ — 전부
> 배제리스트·밸류 델타에 종속된 설계다. ~~`churn_prob_calibrated` → 되살린 밸류에이션의 확률/신뢰
> 축(설계 예정)~~ 도 같다 — 되살릴 대상이 0관측이므로 확률을 붙일 모집단이 없다.
>
> **살아남은 것**: 이 표의 **"계승되는 계약" 열**(AD-5·AD-12·AD-9·AD-4·D1 구조·NFR1)과 아래
> "계승되는 문화"·"계승되는 흉터"는 죽지 않았다. 레아 §4가 이식 계약 9건을 **"산다"**로 판정한
> 것과 같은 이유다 — 이들은 우리 가설이 아니라 데이터의 성질을 인코딩했다.

**계승되는 문화**: 실측 사전조사 · 3레이어 병렬 코드리뷰 · `deferred-work` 기록 · 동어반복 금지
테스트(성질·오라클·변이) · 문서가 테스트를 앞서지도 뒤처지지도 않기.

**계승되는 흉터 (제일 비싼 교훈)**: 3-3 함정 4 — 두 parquet의 행 순서가 다른데 둘 다 RangeIndex라
`Index.equals`를 통과해 **합계가 37% 부풀었고 아무 가드에도 안 걸렸다.** 스크리너의 데이터는 공시·
peer·가격 테이블을 **조인하고 또 조인**한다. 식별자 정합이 깨지면 조용히 틀리는 정도가 crm의 열
배다. 이 상처를 흉터로 새겨 본공연에 가져간다 — **모든 결합은 위치가 아니라 라벨(식별자)로.**

## 4. 옮겨지지 않는 것

- **Epic 2 (BG/NBD LTV 데모) — 무기한 동결.** painpoint와 겹치는 원자가 0이고, 리허설 가치도
  이미 E1·E3가 방법론 근육을 만들며 소진됐다. "계획에 있어서" 하는 것은 이유가 아니다. 해동하려면
  **painpoint 관점의 새 근거**가 있어야 한다.

## 5. 서명이 지배하는 방식 — 판정하지 말고 격차를 드러내라

서명 **"불확실을 확실로 세탁하지 않는다"**는 이 프로젝트에서 장식이 아니라 **병의 이름**이다.
워싱이 하는 짓이 정확히 세탁이다 — 실재하는 리스크(불확실)를 깨끗한 숫자(확실)로 편집한다.

**핵심 함정 (레아 원칙)**: "이 회사는 분식이다"라고 **라벨을 붙이는 모델**을 만들면, 불확실한
추론을 확실한 판정으로 세탁하는 것 — **내가 워싱하는 것**이다. 소송감이고, 틀린다. 게다가 "고의
누락"은 정의상 데이터에 없고, "고의"와 "중요하지 않아서 뺀 것(immateriality)"은 겉으로 똑같다.

따라서 산출물은 **의도를 판정하지 않는다.** 격차를 드러내고 판단을 소비자에게 넘긴다:

> *"동종업계 12곳 중 11곳이 리스 부채를 공시한다. 이 회사는 안 한다. median 가정으로 되살리면
> EV/EBITDA가 6.2에서 9.8로 뛴다. 판단은 당신."*

> **🔴 폐기 표시 (2026-08-03) — 위 예시 문장의 밸류에이션 절.** 원문은 지우지 않는다.
> ~~*"median 가정으로 되살리면 EV/EBITDA가 6.2에서 9.8로 뛴다."*~~ — **폐기.**
> 사유: 이 문장이 전제하는 **"되살릴 금액"이 74곳 검증에서 0관측**이다(C1 🔴).
> 잔액 개념에서 경제적으로 존재하는 금액은 감사 총액 밖에 있을 수 없고(대차 오차 중앙값
> 0.005%), 그러므로 되살림은 **금액의 복원이 아니라 분해의 복원**이었다.
> 근거: `docs/rescope-2026-08-03-leah-domain-verdict.md` §1-1(c) · §3 C1.
> **앞 절("동종업계 12곳 중 11곳이 리스 부채를 공시한다. 이 회사는 안 한다")과 맺음
> ("판단은 당신")은 죽지 않았다** — 격차를 드러내고 판단을 넘긴다는 서명 자체는 유효하다.
> 다만 그 격차는 **기업가치가 아니라 비교의 비용**이며(레아 G2, 협상 불가 조항),
> 이것을 투자 신호로 파는 순간 그것이 세탁이다.
>
> **아래 "배제" 문단도 함께 폐기된다** — 배제리스트는 §3·§6과 같은 사유로 죽었다.
> 다만 그 안의 명제(*"입증 부담이 '이 회사가 틀렸다'가 아니라 '나는 이 공시로 확신할 수 없다'"*)는
> 산출물이 아니라 **한계 문장**으로 살아남는다: 레아 §4는 ①을 *"점수를 받는 항목이 아니라
> 「배제하지 못함」이라는 한계 문장으로만 남아야 한다"*로 판정했다.

배제는 유죄 판결이 아니라 **자기 자신에 대한 정직**이다 — 입증 부담이 "이 회사가 틀렸다"(공매도)가
아니라 **"나는 이 공시로 확신할 수 없다"**(패스)라 서명과 정합한다. 불확실을 불확실인 채로
**행동**으로 바꾸는 것.

## 6. ~~지금까지 확정된 출력 설계~~ — 🔴 **전면 폐기 (2026-08-03)**

> **제목의 "지금까지"는 2026-07-22~29 시점의 현재형이다.** 그 시점 이후 이 절의 두 산출물은
> 전부 죽었다. 제목·본문 어느 것도 지우지 않는다(§2) — 아래 처분을 붙인다.
>
> | 항목 | 처분 |
> |---|---|
> | ~~**① `opacity_rank`**~~ | **폐기(2026-08-03).** 1차 사망은 2026-07-31 마감(§9)에서 **미구현 처분**, 2차이자 확정 사망은 이 날짜의 **회계적 사망 진단서**다 — 전제(총액 밖 복원 대상)가 **C1 0관측**. 레아 §2가 격차 후보에서 명시적으로 뺐고(*"전제가 0관측이다"*) §4가 *"죽음 확정 — 재확인"*으로 적었다. 근거: `docs/rescope-2026-08-03-leah-domain-verdict.md` §2·§4 |
> | ~~**곁에 되살린 밸류 델타가 크기축으로 동행**~~ | **폐기(2026-08-03).** §3 폐기 표시와 동일 사유 — G1 피해 측정 미달(V2)로 밸류에이션 축에 살아 있는 격차가 없음이 확정. 근거: `docs/g1-damage-measurement-results.md` §10, 사전등록 §6-1 3행(*"밸류에이션·투자 신호 문언 전면 폐기가 전제"*) |
> | ~~**② 배제리스트**~~ | **폐기(2026-08-03).** 심장인 배제 운용의 전제("①진짜 누락이 상당수 존재")가 미성립 — 이미 §8에서 IC-memo·mandate.md가 같은 사유로 취소됐다. 근거: 같은 문서 §4 |
>
> **🔒 폐기되지 않은 것 — 옮겨 심는다.** 이 절이 죽어도 아래 두 조항은 살아서 후속 산출물에
> 붙는다. 둘 다 레아 조건이며 협상 불가다.
> - **"미배제 ≠ 클린 인증"** — 업계 전체가 다 숨기면 전원 통과한다는 맹점 명시. §9-1의
>   *"확인 누락 0 / 탐지력 검증 불가 영구 단서"*가 이것의 후신이다.
> - **"모든 행에 이유 한 줄 필수 — 이유 없는 배제는 블랙리스트"** — 관측값 동반 의무(계약 9)로
>   이어졌다. 레아 §4: *"근거 없는 순위는 신탁"*. ①에 붙일 관측값은 정의상 **부재**이고
>   **부재는 관측값이 아니므로**, ①은 순위조차 받지 않는다.
>
> **아래 ⏮ 복원 기록(2026-07-29)의 지위**: 그것은 이 절을 *복원*한 기록이고, 이 폐기 표시는
> 그보다 **뒤에 온 별개의 처분**이다. 복원 기록을 무효화하지 않는다 — 2026-07-29에 복원이
> 옳았다는 판단과, 2026-08-03에 새 관측으로 폐기된다는 판정은 양립한다.

**① `opacity_rank`** (파티 명명, daria 확정 2026-07-22)
- 크레딧 심사 실무 jargon (Morgan 2002 split-ratings; "too opaque to underwrite"). 학술 정본
  `info_asymmetry`(Akerlof·Myers–Majluf)보다 **심사 결정의 화법**에 맞아 채택.
- **순위 전용**, 확률 아님 (AD-5 계승). "83% 워싱"으로 읽는 순간 오용 → 이름·계약·테스트로 봉인.
- docstring 계약: **"peer 공시 격차를 proxy로 측정한 순위"** — 이름은 의도를 전달하고 계약은
  측정을 정직하게 말한다(이름과 측정의 간극 명시).
- 곁에 **되살린 밸류 델타**가 크기축으로 동행 (`expected_saving`의 등가물).

**② 배제리스트**
- 성격: 유죄 판정이 아니라 "이 공시 수준으론 밸류 신뢰 불가 → 패스"라는 자기 결정.
- 모든 배제 행에 **이유 한 줄 필수** ("리스 부채 미공시 — 동종 12곳 중 11곳 공시"). 이유 없는
  배제는 블랙리스트.
- 머리말에 한계 명시: **미배제 ≠ 클린 인증.** 우리 방법은 "동종업계는 공시하는데 얘만 누락"만
  잡는다. 업계 전체가 다 숨기면 전원 통과한다 — 이 맹점을 적지 않으면 그게 세탁이다.

> **⏮ 복원 기록 (2026-07-29, daria 지시)** — 이 절은 같은 날 "공시 표준화 품질 지표"로 전면
> 개정됐다가 **원상 복구**됐다. 개정 근거였던 *"진짜 누락 0곳"*이 아래 이유로 **결론이 되기에
> 이르다**고 판단됐기 때문이다:
> - **탐지력 미검증** — "누락이 없다"와 "우리 방법이 못 찾는다"를 아직 구분하지 못한다(양성 대조군 부재)
> - **표본 강도가 개념마다 다르다** — 무형자산은 전수(거의 확정)지만 **리스는 12곳으로 상한 25%**,
>   주식보상은 의도적 추출이라 구간 산출 불가. 46곳 합산은 추출 설계가 달라 **통계적으로 무효**
>
> 즉 **방향 전환을 정당화한 결론 자체가 지금 보완 대상**이다. 결론이 굳기 전에는 설계를 바꾸지
> 않는다. 폐기했던 "공시 표준화 품질 지표"안은 **결론 확정 후 재검토할 대안**으로만 남긴다.

## 7. 결정 대장 (2026-07-24 파티에서 2건 종결)

**✅ 종결 — 배제 임계의 소유권 (daria 비준 2026-07-24)**
**config 고정 + 감도 annex.** 화면 다이얼 기각 — 같은 데이터에서 애널리스트마다 다른 배제리스트가
나오는 순간 AD-12가 막았던 사고("대시보드에선 Save 우선, 캠페인에선 제외")의 재현이다. 구성:
- 임계는 config 단일 소유, `# source: 정책가정` 라벨 (crm retention 0.30 방식).
- **감도 annex**: 임계 ±스윕 시 배제리스트 반전 구간을 별지로 (crm 3-4 D2 b′ 계약 이식 —
  official 산출물 오염 0을 테스트로 단언).
- 배제리스트 머리말 "**미배제 ≠ 클린 인증**" 협상 불가 (레아 조건).
- 맨데이트별 차이는 다이얼이 아니라 **mandate.md가 임계값과 근거를 서술**하는 것으로 해소
  (Sally 전리품: annex가 official처럼 보이지 않게 하는 화면 규칙은 UX 소유).
- 이 비준으로 **mandate.md 작성 가능 상태** 진입 (8절 선행조건 절반 해소).

**✅ 종결 — 누락 지표 좁히기의 방식 (daria 동의 2026-07-24)**
**이론 열거 금지, peer 공시 격차 실측으로 도출** (1-7 교훈 이식 — SPEC의 "한도 불만형" 예시가
실증에서 부정된 선례). 종전에 이 절에 나열했던 후보(리스·SBC·집중도·충당부채·non-GAAP·
related-party)는 **가설 풀로 강등** — 주인공은 peer 커버리지 격차가 실측으로 벌어지고 밸류에이션
연결(되살린 델타)이 성립하는 지표군이 데이터로 명명한다. 이 실측이 곧 스크리너의 첫 스토리
(crm 1-2/1-3의 등가물)다.

**✅ 종결 — 실측 원료 = SEC (daria 결정 2026-07-28)**

**SEC EDGAR를 실측 원료로 채택.** DART 기각, 병행 기각.

- **채택 근거 (결정적)**: 미국은 XBRL phase-in이 2011년에 끝나 **전원 detail tagging** 상태다.
  DART는 주석 XBRL이 **자산 규모 코호트로 단계 도입 중**(2조↑ → 2024년 사업보고서부터 5천억↑ →
  2025년부터 5천억↓, 디테일태깅은 2천억~5천억이 2025년 기말부터). 한국에서 지금 업종만으로 peer를
  잡으면 **우리가 재는 격차의 상당 부분이 워싱이 아니라 규제 도입 시차**다 — 그걸 `opacity_rank`에
  실으면 "규제 단계"를 재놓고 "불투명"이라 라벨하는 것이고, 이는 **서명 위반**(불확실→확실 세탁).
  레아 판정. 이 프로젝트의 목적상 **레짐 교란이 없는 판**이 도메인 친숙도를 이긴다.
- **부수 이점**: `xbrl/frames` API가 한 개념·한 기간에 대해 **보고한 전 filer의 값을 한 번에** 준다
  — peer 커버리지 격차 측정이 groupby 한 번이다. 2009년 이후 벌크(Financial Statement Data Sets)도 공개.
- **기각 — 병행**: 조인 폭발(함정4 확대판, Winston) + 한 문서가 두 방법론을 설명하면 아무것도
  설명 못 함(Paige). 배제리스트 머리말이 두 갈래로 갈리는 순간 독자는 각주를 안 읽는다.
- **비용 인정 (John)**: daria의 커리어 맥락은 한국 금융권이고 SEC 스크리너는 *"우리 종목엔 되나요"*를
  맞는다. **방법론 증명 우선**으로 감수한 트레이드오프이며, DART 이식은 본공연 이후 별건이다.

**🔑 이 결정이 낳은 계약 — peer 정의는 공시체계에 종속된다 (파티 최대 발견 2026-07-28)**

두 안건(공시체계 · peer 정의)은 독립이 아니라 **커플링**돼 있었다. peer는 "같은 업종"이 아니라
~~"같은 업종 × 같은 공시 레짐 코호트"~~여야 한다 — 레짐을 통제하고 나서야 남은 격차가 비로소
기업의 선택이다.

- SEC에도 레짐 축은 **있다**: **Smaller Reporting Company**(public float < $250M 등)는 Regulation
  S-K/S-X **scaled disclosure**를 항목별로 선택 가능. DART의 자산밴드보다 약하지만 0은 아니다.
  → 코호트 축 = filer status(SRC/accelerated 구분), 자산밴드가 아니다.
- **확정 계약**: `opacity_rank`는 **레짐 통제 후 잔차만** 잰다. 통제 전 원지표를 official 산출물에
  싣지 않는다 (crm 3-4 D2 annex 분리 계약 이식 — official 오염 0을 테스트로 단언).

> **⚠️ 정정 (같은 날 오후, 스파이크 T2 실측)** — 위 취소선 부분은 **틀렸다.** peer는
> **SIC4 단독**이고, filer status는 **셀 분할자가 아니라 보정 공변량**이다.
> **사유**: 대상 개념이 ASC 842/350/718 = 전 등록사 동일 적용이라 SRC scaling(Reg S-K/S-X 서식)이
> GAAP 주석을 면제하지 않는다 → 규제 교란 성분 ≈ 0. 그런데도 셀을 쪼개면 불투명성이 규모와
> 상관되는 만큼 **사냥감을 통제가 지운다**(과잉통제). 대가도 실측됐다 — 셀 분할 시 n<10에 갇히는
> filer가 19.8% → **39.4%**로 두 배.
> **계약 문언 자체는 처음부터 옳았다**: "레짐 통제 후 **잔차**" — 층화가 아니라 보정이라고 이미
> 쓰여 있었고, T1이 잰 39.4%는 그 계약의 잘못된 구현에 붙은 비용이었다.
> 레아가 2주 전 자신의 요구를 이 자리에서 철회했다("판이 바뀌면 조건도 다시 봐야 한다 — 안 그러면
> 조건이 관성이 된다"). 상세·반증 설계: `docs/screener/spike-1-t2-peer-definition.md`.

**🔒 신규 계약 — 부재의 정의 (스파이크 T2, Winston)**

**부재는 두 데이터셋(재무제표 벌크 · 주석 데이터셋) 모두에서 부재일 때만 부재다.** 단일 소스 부재는
"부재 후보"일 뿐이며, 이 규칙은 문서가 아니라 **테스트로 고정**한다.
근거(실측 2025Q1): `OperatingLeaseLiability` 커버리지가 face only **6.0%** ↔ notes set **79.4%**
(+73.4pp). 표준 데이터셋만 봤으면 **약 94%의 filer를 "누락"으로 배제리스트에 실명 등재**할 뻔했다.

**✅ 종결 — 문제정의 재정의 후 방향 (파티 2026-07-29)**

정본 문제정의(§1) 확정 직후 파티가 4건을 정했다.

1. **산출물 구조 전환**: **3분류 테이블(①진짜 누락 / ②다른 곳 기재 / ③애초에 0)이 1차 산출물**,
   `opacity_rank`는 ①에서 파생되는 **2차** 산출물이다. 종전 설계(§6)는 순위가 1차였다 — 순서가
   뒤집혔다. 근거: 재정의를 그대로 읽으면 3분류가 목적이며, "미배제 ≠ 클린 인증" 경고가
   테이블 구조 자체에 내장된다(Paige).
2. **다음 작업 = 재무비율 영향 측정.** 최종 질문 셋(기업가치·재무비율·투자자 판단) 중 스파이크 1의
   K3는 **기업가치 하나만** 쟀다(되살림÷시총). **재무비율은 미측정**(John 지적). 기존 데이터로
   측정 가능하며 가격 API 불필요(Winston). 크레딧 관점에선 시총보다 이쪽이 본질이다 — 심사는
   부채비율·이자보상배율로 본다(레아). **K3 결론을 뒤집을 수 있는 측정이다**: 시총 대비 1% 미만이라
   기각한 회사가 부채비율에서는 크게 흔들릴 수 있다(분모가 다르다).
3. **SBC 이중 처분**: 스크리너 대상 **제외 유지** + **③(애초에 0) 판별의 검증 사례로 채택**.
   밸류에이션 질문에서는 죽었으나(전부 0이면 정상화해도 안 움직인다) 분류기가 작동한다는 증거로는
   최상이다 — 10-K 열람으로 ③을 실제 확인했다. 모순이 아니다(John).
4. **"투자자 판단"은 IC-memo 워크드 이그잼플로 답한다** — 지표가 아니라 화면·문서로 재는 항목
   (Sally). 스캐폴딩 이후.
5. **새 개념 추가는 재무비율 측정 후로 연기** — 이미 가진 개념 2개에서 안 짜낸 답이 남아 있다.

**⏮ 철회 — 프로젝트 방향 C안 (daria 지시 2026-07-29, 같은 날 복구)**

세 개념 전멸(개별 검증 46곳 중 진짜 누락 0곳) 직후 **A+C**로 결정했으나, **C는 daria가 철회**했다.

- **A — 결론 출판**: 유지. 단 **최종 결론이 아니라 중간 결과**로 성격을 낮춘다(`FINDINGS.md`).
- **C — 표적 전환(누락 탐지 → 비표준 보고 탐지, 산출물을 공시 표준화 품질 지표로)**: **철회.**
  `opacity_rank`·배제리스트·되살린 밸류 델타 원설계를 §6에 복원.
- **철회 사유(논리)**: C의 근거는 *"진짜 누락이 거의 없다"*였는데, 한계 보완 파티가 **그 결론
  자체가 아직 약하다**는 것을 보였다 — ①**탐지력 미검증**(양성 대조군 부재로 "없다"와 "못 찾는다"를
  구분 못 함) ②**표본 강도 차등**(무형자산 전수 = 거의 확정 / 리스 n=12 → 상한 25% / SBC 의도적
  추출 → 구간 불가) ③46곳 **합산은 추출 설계가 달라 통계적으로 무효**.
  **결론이 굳기 전에 설계를 바꾸지 않는다.**
- **B(개념 추가) 기각은 유지** — 3연속 0.
- C안은 **폐기가 아니라 보류** — 탐지력 검증 후 결론이 확정되면 재검토할 대안으로 남긴다.

**✅ 종결 — crm 기술부채의 본공연 이식 판정 (파티 2026-07-31, daria 판정)**

crm `deferred-work.md` 미해소 항목 전수를 이식 관점에서 판정했다. **crm은 동결 유지**(401 passed
상태가 자산) — 프레임은 "crm을 고치나"가 아니라 "이식 계약에 무엇을 추가하나"(Winston).

- **계약 +1 = 데이터 계보 및 무결성 계약(가칭 — 정식 명칭은 jargon 확인 후 daria 확정)**.
  레아(정체성)·그럼발(신선도) 요구를 별개 계약 2건이 아니라 **한 계약의 두 조항**으로 통합
  (Mary의 계약 인플레이션 우려 해소). 이식 계약은 8건 → **9건**.
  - **조항 1 — 행 단위 정체성**: 모든 결과 행은 **CIK–accession**의 검증된 연결 필수.
    **ticker는 가격 축을 읽는 행에만 필수**, 부재는 `no_market_quote` 명시 라벨(레아 수정 —
    OTC 29곳의 티커 부재는 데이터의 사실이므로 행 필수로 박으면 계약이 세탁을 강요한다).
    실증 근거: 3-3 행 오정렬 +37.2% 무예외 · 스파이크5 FTS 오짝짓기 8/28건 — 같은 병.
    crm 4-1b 가드의 "이식"이 아니라 **계층·다대다 키 체계(CIK·accession·ticker)로 재설계**.
  - **조항 2 — 계보·신선도**: 모든 결과 행은 원천 파일 버전·파이프라인 실행 이력 추적 가능.
    **합격선을 테스트로 존재시킬 것: "상위 산출물이 바뀌었는데 하위가 재계산 없이 통과하면
    테스트 실패"**(그럼발 수정 — meta 타임스탬프 기록만으로는 crm이 이미 뚫린 수준).
- **구현 게이트 4단계 (계약 수와 분리)**: ①정체성 정합 — 스캐폴딩 즉시 ②DAG/staleness —
  첫 SEC 파이프라인 실행 전, 후장착이 아니라 **원천 해시가 캐시 키인 구조로 내장**(Winston)
  ③크래시 안전성 — **실명 배제리스트 정식 생성 전** 필수. crm에선 저위험이었으나 산출물이 실명
  리스트가 되는 순간 등급 상승(Sally 발굴): 임시 파일 → 원자적 교체 + 완료 메타에 **"몇 행
  검사했나"** 포함(쓴 행 수만으론 50/100 검사 중단과 100 검사·50 배제를 구분 못 함)
  ④보정 계약 — **확률 모델 채택 시에만 활성화**(Platt sigmoid 가정은 crm 데이터의 우연이지 보장이
  아님 — 모델 변경 = 보정 방식 재검토).
- **보류**: XGBoost 계열 부채(scale_pos_weight 상속·폴드 이중 학습·`_validate_axis` 패턴) —
  스크리너의 모델 결정 전까지 존재하지 않는 코드의 부채는 목록에 넣지 않는다(Mary).
- 기록처: 이 대장이 정본, crm `deferred-work.md`에는 역링크 한 줄만(Paige — crm 문서는 동결이라
  스캐폴딩 때 아무도 다시 안 읽는다).

**✅ 종결 — C안 재검토 = 1안 「정본 회귀 + C′ 합성」 채택 (파티 2026-07-31, daria 판정)**

스파이크5의 수렴 실측("확정 결함 19곳 중 태그 부재 2곳뿐 — 결함은 부재가 아니라 있는데 틀린
값에 산다")을 재료로 보류 중이던 C안을 재검토했다.

- **보류 해제의 논거 교체(레아 조건)**: 원 보류 조건("탐지력 검증 후 결론 확정")은 X1 미달로
  **충족 불가능**해졌다 — 충족 불가능해진 조건을 기다리는 것은 관성이다(그럼발이 레아의 T2
  문장을 레아에게 적용). 단, 재검토 논증에서 미판정 "진짜 누락 0곳"에 기대는 것은 전면 금지 —
  **새로 생긴 양의 증거(왜곡형 15곳)로만** 세웠다.
- **정본 회귀 확정**: 3분류 테이블(①②③)이 1차 산출물이라는 정본 문제정의를 유지·확정한다.
  스파이크 1~4에서 죽은 것은 "①이 많을 것"이라는 기대이지 분류기가 아니다(②③은 정확히
  작동했다). ①의 희소성은 실측 라벨로 산출물에 동행한다.
- **C′ 채택 (C 원안은 기각)**: 표적을 "비표준 보고 → 표준화 품질 지표"(C 원안, 미판정 결론
  의존이라 기각)가 아니라 **"peer 대비 값 이상치 = 검증 우선순위"**(C′, 새 증거 정합)로.
  스파이크5의 왜곡형 15곳이 **양성 대조군으로 전직**한다(daria 판정 (b)의 라벨 분리가 만든 자산).
- **게이트**: C′는 **스파이크6 통과 시에만** 산출물로 승격. 사전등록
  `docs/screener/spike-6-value-anomaly-preregistration.md` — kill criteria Y1(측정 가능 ≥8/15) ·
  Y2(민감도 ≥40%) · Y3(오탐률 ≤15% ∧ lift ≥2.5)를 **측정 전에 고정**(임계가 측정을 앞선다,
  2연속 준수). 미통과 시 C′ 기각, 정본 회귀 단독으로 마감 후보.
- **출력 계약(통과 시)**: 배제리스트가 아니라 검증 우선순위 리스트. 화면 문언 "peer 분포에서
  이만큼 떨어져 있고 확인할 가치가 있다"(Sally 조항 — 고의가 아니라 **거리**를 드러낸다).
  보정 없는 원거리만 측정, 보정 도입은 통과 후 별도 스토리 + T4 연속판 회귀 테스트 선행.
- **기각·보류 기록**: Boundary의 X1 재도전(무형 부재형 2009~2018 창 확장)은 기각이 아니라
  **비우선** — 그럼발의 비용 반론(n=7 민감도의 값어치) 우세. C′ 결과가 나온 뒤 재평가 가능.

**⏳ 열려 있음**
1. **데이터 스파이크 1일차 (착수 가능)**: 원료가 정해졌으므로 스파이크는 별개 작업이 아니라
   **격차 실측의 첫 삽**이다. 스토리: `docs/screener/spike-1-sec-disclosure-gap.md`.
   판정 대상 = "이 데이터로 이게 되긴 하냐"(그럼발 경고) — **kill criteria를 먼저 적고 시작한다.**

## 8. 다음 문서 — 🏁 마감 처분 (daria 비준 2026-07-31)

~~IC-memo · mandate.md~~ — **작성 취소.** 두 문서의 심장인 배제리스트 운용이 전제("①진짜 누락이
상당수 존재") 미성립으로 무산됐다. 조용히 안 쓰는 것은 부채이므로 취소를 명시한다(Mary 조항).

## 9. 🏁 마감 선언 (daria 비준 2026-07-31)

**프로젝트는 "정본 회귀 단독"으로 마감한다.** 최종 산출물:

1. **FINDINGS.md 최종판** — 6형태 분류 체계(주 산출물) + "확인 누락 0 / 탐지력 검증 불가 영구
   단서"(레아 조항, 협상 불가) + "실패했는가" 자문 문단(Sally 조항)
2. **3분류 판정기** — ②③ 실증 작동, ①은 희소 + 탐지력 미검증 라벨
3. **이식 계약 9건** — 데이터 계보·무결성 계약(2026-07-31 신설) 포함. 다음 판의 출발 자산
4. **유산 각주 2건** — C′ 신호(민감도 42.9%·lift 2.6, peer 지형으로 판정 불능 — 본공연급 표본에서
   재검토 가능) · C안(결론 확정 시 재검토)

**지위 변경**: DART 이식 = "본공연 이후 별건" → **후속 프로젝트 후보** (6형태 분류의 이식 명제:
*"분류는 이식 가능하되 형태별 빈도는 다를 것"*). Epic 2 동결 유지.

**소수의견 기록 (Boundary)**: 미탐사 창 2개 — 무형자산 부재형 양성 2009~2018(X1 재도전의
유일하게 알려진 경로) · 리스 개별검증 12→30(상한 25%→10%). 둘 다 기각이 아니라 **비용 대비
판단으로 미실행**. 후속 프로젝트의 재개 지점.

**마감의 성격 (그럼발 문언)**: 패배 선언이 아니라 완주 선언 — 가설이 물을 수 있는 질문은 다
물었고, 데이터가 답할 수 있는 답은 다 받았다. 사전등록 kill criteria가 세 개념과 두 표적 전환을
죽이는 동안 기준은 한 번도 물러서지 않았다.

## 10. 2026-08-03 판정 — §9 마감 선언의 **갱신** (취소가 아니다)

> **§9의 성격을 먼저 정확히 쓴다.** §9(2026-07-31)는 **취소되지 않았다.** "정본 회귀 단독으로
> 마감한다"는 비준은 그대로 서 있고, 그 마감 이후 프로젝트는 스캐폴딩 → 엔진 구축 → 첫 사용
> 검증으로 이어졌다. 2026-08-03의 판정들은 §9를 **뒤집은 것이 아니라 이어받아 갱신**한다 —
> 마감 산출물 4건 중 무엇이 살고 무엇이 죽는지가 이 날 관측으로 확정됐다.
> 이 절은 **새 판정을 만들지 않는다.** 이미 내려진 판정을 문서에 반영할 뿐이다.

### 10-1. 격차 3개의 최종 상태 (레아 판정 + G1 측정)

레아 도메인 판정(`docs/rescope-2026-08-03-leah-domain-verdict.md`)이 격차 후보 3개를 세웠고,
그중 G1의 피해 크기를 사전등록 후 측정했다.

| | 상태 | 근거 |
|---|---|---|
| **G1 — 업종 동음이의로 인한 peer 벤치마크 오염** | 🔴 **사망** | 합격선을 잴 **대상 셀이 0개**(V2 발동 — "미달"이 아니라 "잴 것이 없었다"). D1·D2·D3 전부 미달. 49·63·65·67 네 섹터에서 **섹터 자체가 동음이의 업종**이라 비교군이 없었다 — 오염의 **메커니즘 자체가 데이터에서 성립하지 않았다**. `g1-damage-measurement-results.md` §6·§7·§10 |
| **G2 — 위치·태그 분산으로 인한 기계 비교 불가** | 🟡 **산다 — 그러나 기업가치를 바꾸지 않는다** | 레아 §G2 협상 불가 조항: *"G2는 기업가치를 바꾸지 않는다. 금액은 이미 감사된 총액 안에 있다. 바뀌는 것은 비교의 정확도와 비용이다."* 소비자는 투자자가 아니라 **데이터 소비자** |
| **G3 — 분해 비대칭** | 🔴 **실재성 최상 · 계측기 부재로 도달 불가** | C′가 스파이크6에서 기각 확정, 유한/무한 내용연수는 태그에 존재하지 않음(U-4). 순위 3위는 중요도가 낮아서가 아니라 **손이 닿지 않아서** |

> **귀결**: **밸류에이션 축에 살아 있는 격차가 없다.** 남은 것은 G2 하나이고 G2는
> 밸류에이션 제품이 아니다. 이 확정은 추론이 아니라 **관측 위에 선다** —
> 사전등록 §6-1이 측정 **전에** 적어 둔 귀결이 그대로 발효한 것이다.

### 10-2. 산출물 판정 (레아 §4 — 새로 판정하지 않고 그대로 옮긴다)

**산다**: 부재의 **6형태 분류 체계**(d=0이 오히려 값을 올렸다 — 이 분류의 타당성은 d>0에
의존한 적이 없다) · **이식 계약 9건**(우리 가설이 아니라 공시 데이터의 성질을 인코딩했으므로
표적과 무관하게 산다) · **6형태 순위 소견 출력 구조**(단, ①을 뺀 형태로 — 관측값 동반 의무 유지) ·
**방법론 규율 자체**.

**죽는다 — 표적과 함께**:

| 산출물 | 판정 |
|---|---|
| **① CANDIDATE_OMISSION의 산출물 지위** | 🔴 죽는다. 28곳에서 한 번도 정답이 아니었고 탐지력은 **영구 미검증 확정**(스파이크5 X1 미달). 계약 1은 "확정 출력"을 금지했고 **여기서 한 걸음 더 나간다 — 순위도 주지 않는다.** ①은 **"배제하지 못함"이라는 한계 문장**으로만 남는다 |
| **"3분류 판정기"라는 이름** | 🔴 이름이 죽고 기능이 산다. 정확한 이름은 **「②③ 확인기 + ① 미확정 잔여」**이며 **잔차는 범주가 아니다** |
| **`opacity_rank` · 배제리스트 · 되살린 밸류 델타** | 🔴 **죽음 확정 — 재확인.** §3·§5·§6 폐기 표시가 이것이다 |
| **태그 계층 증거규칙의 추가 회전** | 🔴 **①을 향한 경로로서는** 죽는다(관측 계층의 부재). 단 G1·G2를 향한 규칙은 다른 이야기 |
| **최종 질문의 현 문언** | 🔴 현 표적 아래에서는 죽는다 — **단, 정본 개정은 daria 권한이므로 이 문서는 §1 각주로만 처리했다** |

**애매한 것은 애매한 채로 남긴다**: CRICUT 1칸(d를 배제 못 한 미결) · Boundary 소수의견 2건
(기각이 아니라 미실행 유지 — 둘 다 C1 0관측을 뒤집지 못한다).

### 10-3. 🚫 이 문서에서 인용 금지 — 라벨을 문서에 박는다

> **위험의 구조: 숫자는 표에 있고 라벨은 산문에 있다. 표만 복사하면 라벨이 떨어진다.**
> 아래는 **새기 쉬운 순서**이며 **새면 아픈 순서가 아니다**(둘을 같은 칸에 넣지 말 것).
> 전수 목록은 `docs/open-issues-2026-08-03.md` §2. 이 문서는 그 목록을 **여기에도 박는다** —
> 금지 규율이 금지된 그 문서에만 적히면 반드시 재발하기 때문이다(M5).

| # | 묶어 둔 것 | 떼면 무엇이 되나 (= 잘못) |
|---|---|---|
| **1** 🔴 가장 새기 쉬움 | **post-hoc²(결과-후 인샘플 적합) 등급 라벨** | **"엔진 적중률"**이 된다. 실제로는 **오분류 회사명을 알고 짠 규칙으로 같은 정답지를 다시 푼 성적**이고, 홀드아웃 청정 비동점 적중은 **1건**이었다 |
| **2** 🔴 새면 가장 아픔 | **누적 28곳 d=0** | **"누락은 존재하지 않는다"**로 읽힌다 — 계약 1의 **거울상**이며, 이 프로젝트가 여섯 번 잡아낸 바로 그 오류를 우리 자신이 저지르는 것이 된다. 유보 셋(표본·CRICUT·노출)을 붙인 채로만 인용 |
| **3** 🔴 | **G1 측정의 −46%·−74%·−84%** | **"동음이의가 벤치마크를 최대 84% 왜곡한다"**가 된다. 실제로는 **계약 6 미달 판정 불가 셀**의 값이고 플라시보 대조도 안 받았으며 **부호가 예상과 반대**다. 두 라벨 **「벤더 정규화 이전」·「이동의 상한 — 오류의 크기가 아니다」**는 뗄 수 없다 |
| **4** 🟠 | **n=4~5 비율 환산 · TIE 적중(E-SMART)** | **"홀드아웃 1순위 적중 2/5 = 40%"**가 된다. TIE는 enum 순서의 산물이다. **비율이 아니라 건수로만** 말한다 |
| **5** 🟠 | **계약 7 "① 1순위 0건"** | **"우리 도구는 함부로 누락이라 하지 않는다"**는 겸손의 증거가 된다. 그것은 판단이 아니라 **C-1이 구조적으로 보장**한 결과다 |
| **6** 🟡 | **"산문 없이는 원리적으로 불가"** | 정확한 사실은 **"우리가 본 구조화 계층으로는 불가"**다 — 파이프라인은 차원(axis) 태그와 DQC 채널을 **한 번도 읽지 않았다** |

**이 문서 자신에 적용되는 것**: §3·§5·§6에 남아 있는 밸류에이션 문언(EV/EBITDA 6.2→9.8,
"되살린 밸류 델타", `opacity_rank`)은 **폐기된 설계이며 인용 금지**다. 원문이 읽히도록 남긴 것은
역사 보존을 위해서지 재사용을 위해서가 아니다.

### 10-4. 미결 — 이 문서가 닫지 않는 것

1. **(a) 인터뷰 조달** — 판정 시점 **2026-08-10 24시**(S1, 달력 임계·이동 불가). (b)가 미달
   확정이므로 귀결표의 남은 미지수는 (a) 하나뿐이고, (a) 미달이면 **선택지 1(마감) 확정**이 발효한다.
2. **최종 질문의 처리** — §1 각주, daria 판단 대기.
3. **계보 계약의 정식 명칭** — **"가칭"으로 명기하고 종료**한다. 이름을 창작해서 채우지 않는다.
4. **선택지 3(표적 교체)은 데이터로 기각된 적이 없다** — 마감 시 "검토되고 기각됐다"로 요약하면
   그것은 사실이 아니다.

## Change Log

| 날짜 | 변경 |
|---|---|
| 2026-08-03 (밸류에이션 문언 폐기) | **§10 신설 + 현재 상태 배너 + 폐기 표시 3건(Paige, daria 지시).** §9 마감 선언(07-31)의 **취소가 아니라 갱신** — 07-31 이후 확정된 것을 §10에 기록. **밸류에이션 축 전면 폐기**: G1 피해 측정이 대상 셀 0개로 **V2 발동·D1D2D3 전부 미달**(`g1-damage-measurement-results.md`) → 사전등록 §6-1 귀결표 3행(*"밸류에이션·투자 신호 문언 전면 폐기가 전제"*)이 발효. §3 부품 매핑의 `opacity_rank`·배제 판정·**되살린 밸류 델타** 행, §5 예시의 *"EV/EBITDA 6.2→9.8"*, §6 "지금까지 확정된 출력 설계" ①②에 **폐기 표시**(§8 Mary 조항 형식 — 원문 삭제 없음, §2 소급 재작성 금지 준수). 격차 최종 상태 G1 사망 · G2 생존(기업가치 불변, 데이터 품질) · G3 도달 불가. 산출물: 6형태 분류·이식 계약 9건·순위 소견 구조(①제외)·방법론 규율은 **산다**, ① 산출물 지위·"3분류"라는 이름·`opacity_rank`·배제리스트·밸류 델타는 **죽는다**(레아 판정 그대로 반영, 신규 판정 없음). **최종 질문은 고치지 않고 §1 각주로 daria 판단 요청**(정본 개정은 daria 권한). **인용 금지 라벨 6종을 §10-3에 박음**(post-hoc² 등급 · 28곳 d=0 · G1 −84% · n=4~5 비율 · "①1순위 0건" · "산문 없이는 원리적으로 불가"). 미결: (a) 인터뷰 S1 = 2026-08-10 24시. |
| 2026-07-31 (마감) | **🏁 마감 비준(daria).** 정본 회귀 단독 — FINDINGS 최종판(탐지력 검증 불가 영구 단서 + "실패했는가" 자문 문단) · 3분류 판정기 · 6형태 분류 체계 · 이식 계약 9건. IC-memo·mandate.md 작성 취소(전제 소멸 명시). DART = 후속 프로젝트 후보로 지위 변경, Epic 2 동결 유지. 소수의견 2건(무형 2009~2018 X1 재도전 · 리스 12→30) "비용 판단 미실행"으로 등재. 성격 = 완주 선언: kill criteria가 개념 3 + 표적 전환 2를 죽이는 동안 기준 불변. |
| 2026-07-31 (C′ 기각) | **스파이크6 종결 — C′ 기각 확정.** 재실행(주석 데이터셋 정정, 1회 한정)에서 Y1 7/15 미달·Y3 오탐 16.5% 미달(Y2 42.9% 통과) → §8-3 조항 발동. 근소 미달 반올림 금지(K1 소급 금지 동일 원리). 유산 기록: 배관 정정으로 민감도 16.7→42.9%·lift 0.91→2.6 — 신호는 0이 아니나 peer 셀 기아(왜곡 기업 절반이 peer<10 지대)로 판정 불능 — 본공연급 표본에서 재검토 가능. 정본 회귀 단독이 마감 후보로 진입(마감 판정은 daria). |
| 2026-07-31 (C′) | **C안 재검토 종결 — 1안 「정본 회귀 + C′ 합성」 채택(daria).** 3분류 테이블 = 1차 산출물 확정(① 희소성은 실측 라벨). C 원안 기각(미판정 결론 의존), **C′ = peer 대비 값 이상치 탐지**를 스파이크6 게이트로 채택 — 왜곡형 15곳이 양성 대조군으로 전직. 사전등록 커밋(Y1≥8/15·Y2≥40%·Y3 오탐≤15%∧lift≥2.5, 측정 전 고정). 스파이크5 판정도 같은 날 종결: X1 미달(부재형 4<5) → 탐지력 검증 불가 기록, "진짜 누락 0곳" 미판정 유지. 수렴 발견: 결함은 부재가 아니라 "있는데 틀린 값"에 산다. |
| 2026-07-31 | **7절 신규 종결 — crm 기술부채 이식 판정(파티, daria).** 계약 +1 「데이터 계보 및 무결성 계약(가칭)」= 정체성(CIK–accession 행 필수, ticker는 가격 축만+`no_market_quote` 라벨 — 레아 수정) + 계보·신선도("상위 변경 시 하위 미재계산 = 테스트 실패"를 합격선으로 — 그럼발 수정) 두 조항 통합. 이식 계약 8→9건. 구현 게이트 4단계(정체성→staleness→크래시 안전성→보정), XGBoost 계열 부채는 모델 결정 전까지 보류(Mary). crm은 동결 유지(Winston). |
| 2026-07-29 (재정의) | **daria가 문제정의를 정본으로 갱신** — "워싱 기업 배제"에서 **"표준 XBRL 금액이 안 잡히는 회사를 ①진짜누락/②다른곳기재/③애초0으로 3분류"**로. 유죄 함의 제거(레아 원칙 내장), 3분류가 산출물 그 자체, 최종 질문이 기업가치+**재무비율**+투자자 판단 셋. 제목도 「재무공시 누락·비표준 보고 스크리너」로 변경. 파티 결론 5건: 3분류 테이블이 1차 산출물(opacity_rank는 2차) · 다음 작업=재무비율 측정(미측정 발견) · SBC 이중 처분 · 투자자 판단은 IC-memo로 · 새 개념은 연기. |
| 2026-07-22 | THESIS 초안. painpoint 재정의(파티 5세션) 후 through-line 선언 — crm은 워싱 스크리너의 방법론 리허설. 부품 매핑 표, Epic 2 동결, 서명이 "격차를 드러내되 판정 안 함"으로 작동하는 방식, 확정된 출력 설계(opacity_rank + 배제리스트), 열린 결정 3건(임계 소유권·누락 지표·데이터 스파이크). IC-memo·mandate는 후속 파티 후 작성. |
| 2026-07-29 (판정) | **스파이크 판정 3건 종결(파티, daria 전권 위임).** ①**K1 임계 = 소급 설정 영구 금지**(레아: 위임이 세탁을 합법화하지 않는다) + 전방 기준 설정 — *새 개념은 개념별 부재 정의 후 잔차>0 ∧ 개별 검증 생존 실명≥1, 미달 시 그 개념 제외*. 비율 임계 기각(분모 붕괴에 놀아남, 216% 사건). ②**peer 최소 셀 = n≥10 확정**(실측: 승급 후 사냥감 탈락 3.5%. n≥5는 레아 조건 위반, n≥20은 사냥감 10% 잠식). config 고정+정책가정 라벨+감도 annex(5/20 병기). ③**조건부 속행** — 3번째 개념 SBC(주식보상, 손익 구조라 기존 2개념과 상이), ①의 전방 기준을 착수 전 kill criteria로 적용 → 통과 시 스캐폴딩 → `opacity_rank` 첫 스토리. 그럼발의 중단론은 "짧은 리스트 = 개별 검증 가능한 리스트"(레아)로 철회. |
| 2026-07-28 (오후) | **스파이크 T1·T2 실행.** T1: SEC 3종 취득 경로 확정, **주석 레벨 도달**(축소 불필요), 함정 3건(주석 데이터셋 2025Q3 분기→월간·`.tsv`·단일 월 계절편향). T2: **peer = SIC4 단독으로 정정**(오전 결정의 "업종 × 레짐 코호트"는 과잉통제 — 규제 성분 ≈ 0), filer status는 보정 공변량. **신규 계약**: 부재는 두 데이터셋 모두에서 부재일 때만 부재(face only는 `OperatingLeaseLiability`를 6.0%로 봐 94%를 오인할 뻔). 레아가 자기 조건 철회. |
| 2026-07-28 | **7절 3번째 결정 종결 — 실측 원료 = SEC**(daria, 파티 A4). DART 기각 근거는 편의가 아니라 **레짐 교란**: 주석 XBRL이 자산 코호트로 단계 도입 중이라 업종만으로 peer를 잡으면 규제 시차를 워싱으로 오독한다(레아). 병행 기각(조인 폭발·문서 이원화). **파티 최대 발견 = 두 안건의 커플링**: peer = 업종 × 레짐 코호트, SEC에서는 코호트 축이 filer status(SRC scaled disclosure). 신규 계약: `opacity_rank`는 레짐 통제 후 잔차만 잰다. 스파이크 1일차 스토리 착수 가능. |
| 2026-07-24 | 7절을 결정 대장으로 재구성 — daria 비준 2건: ①배제 임계 = config 고정+정책가정 라벨+감도 annex(3-4 계약 이식)+미배제≠클린 머리말, 다이얼 기각. mandate.md 작성 가능 상태 진입. ②누락 지표는 이론 열거 금지, peer 공시 격차 실측 도출(1-7 교훈) — 종전 후보 목록은 가설 풀로 강등. 남은 열림: 실측 원료(공시체계·peer 정의, daria) → 데이터 스파이크가 실측 1일차로 통합. 4-1a done(함정4 방어 = 3절 흉터 계승 완결) 반영. |
