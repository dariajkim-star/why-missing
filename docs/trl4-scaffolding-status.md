# TRL 4 스캐폴딩 — 1차 완료 기록 (2026-07-31)

## 무엇을 세웠나

`adjudicator/` 패키지 — **순수 판정 모듈만**. 어떤 모듈도 import 시점에 네트워크를 타지 않는다
(crm AD-1/AD-9 순수성 규율 계승).

| 모듈 | 담당 계약 |
|---|---|
| `config.py` | 상수 단일 소유 + `# source:` 라벨 (AD-4 계승) — peer n≥10은 실측 근거 주석 |
| `concepts.py` | 계약 1 (개념별 부재 정의) — **흐름 개념 입구 거부**(⑤ 기계 불가), 동음이의 목록 "성장 중" 라벨 |
| `verdict.py` | 6형태 판정 + 출력 문언 — **"누락 확정" 출력 불가**(M4), 근거 한 줄 필수 |
| `peers.py` | 계약 6 — SIC4 → SIC3 승급 → **판정 불가**(억지 셀 금지) |
| `identity.py` | 계약 9 조항 1 — CIK–accession 정규형 강제, **404 ≠ 조회 실패** |
| `freshness.py` | 계약 9 조항 2 — 내용 지문 기반, 시계(timestamp) 아님 |
| `safewrite.py` | 크래시 안전성 — 원자적 교체 + **"몇 행 검사했나"** 메타 |
| `sanity.py` | 계약 3 — 큰 회사가 상위권이면 자기 의심 (위치를 반환해 지목 가능하게) |

## 테스트 — 27 passed

`tests/test_contracts.py`(계약별) · `tests/test_six_forms.py`(6형태, 실사례 오라클).
각 테스트는 **자기가 죽이는 변이를 주석에 명시**한다(동어반복 금지 규율).

### 변이 검증 8종 — 전부 KILLED

| 변이 | 결과 |
|---|---|
| 동음이의 검사 제거 | KILLED |
| face 데이터셋만 읽기 | KILLED |
| 증거 불명을 부재로 추정 | KILLED |
| 억지 peer 셀 허용 | KILLED |
| 조회 실패를 부재로 처리 | KILLED |
| staleness 미탐지 | KILLED |
| 크래시 훅 무력화 | KILLED |
| 흐름 개념 통과 | KILLED |

**살아남은 변이 0건** — 테스트가 장식이 아님을 실측으로 확인.

## 설계 판정 하나 (기록)

**동음이의(⑥) 검사가 부재(①) 검사보다 먼저 온다.** Warrior Met Coal은 두 조건을 동시에
만족하는데, 순서가 반대면 "존재하지 않는 $131.9M 부채를 만들어낸" 그 사고가 코드로 재현된다.
`test_homonym_outranks_candidate_omission`이 이 순서를 고정한다.

## 남은 것

- **M1 — 스파이크4 무형자산 전수 23곳 골든 재현** (판정 동일성). SEC 데이터 수집 계층이
  필요하므로 다음 작업 단위.
- M3 — 미학습 신규 회사 1곳 30초 내 입출력 (CLI 필요)
- 레거시 스크립트 5종(`scripts/`)의 패키지 흡수 — M1 작업 중 자연 해소

## 계약 커버리지 현황

| 계약 | 상태 |
|---|---|
| 1 개념별 부재 정의 | ✅ 테스트 고정 |
| 2 두 데이터셋 부재 | ✅ 테스트 고정 |
| 3 큰 회사 자기의심 | ✅ 테스트 고정 |
| 4 공변량 보정(층화 금지) | ⏳ 보정 도입 시 (C′ 기각으로 현재 비활성) |
| 5 금액 축 동행 | ⏳ 금액 축 재도입 시 |
| 6 peer 정의 | ✅ 테스트 고정 |
| 7 OTC 물리분리 | ⏳ 모집단 계층 도입 시 |
| 8 SEC 취득 규약 | ⏳ M1 (수집 계층) |
| 9 계보·무결성 | ✅ 조항 1·2 모두 테스트 고정 |
