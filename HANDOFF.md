# 인계 — 2026-07-31 (why-missing 첫 세션용)

> **이 문서를 읽는 새 세션에게**: 이 저장소는 `crm-targeting-lab`(리허설, 동결)에서 오늘 이사
> 왔다. 아래 순서대로 읽고 시작할 것. **다음 작업은 코드가 아니라 사용 검증이다.**

---

## 0. 복명복창 (파티 소환 시마다 — daria 상시 지시)

> **기업 공시에서 특정 재무 개념이 경제적으로 존재하는데도 표준 XBRL 금액이 잡히지 않는 회사를
> 찾아서, ①진짜 미공시·누락인지 ②다른 곳에 적힌 것인지 ③애초에 0인 것인지를 구분하는
> 재무공시 누락·비표준 보고 스크리너를 만든다.**
>
> **최종 질문**: 누락되거나 비표준적으로 보고된 금액을 **정상화하면 기업가치·재무비율·투자자
> 판단이 실제로 달라지는가?**

---

## 1. 지금 어디에 서 있나 (2026-07-31 판정)

**기술과 제품의 성숙도가 다르다. 섞어 읽지 말 것.**

| 대상 | TRL | 근거 |
|---|---|---|
| **분류 엔진**(6형태 판정) | **4** | 스파이크 1~6 실데이터 검증 + M1 골든 재현(23곳 인간 판정 대조), 테스트 53·변이 19/19 KILLED |
| **제품 개념**("부재 설명 도구") | **1~2** | 어제 파티에서 정의됐을 뿐, **실사용자가 한 번도 안 써봤다.** "쓸모 있는가"는 완전 미검증 |

### 🔴 이 프로젝트가 지금 밟고 있는 위험

원래 개념("워싱 잡는 그물")은 스파이크 1~6에서 **기각**됐다. 그 자리에 세운 새 개념
("부재 설명 도구")은 **TRL 1~3을 건너뛰고** 곧바로 MVP 코드(TRL 5~6급)로 갔다.
지금까지의 M1·M3 성과는 전부 *"만들 수 있다"*의 증명이지 *"이게 필요하다"*의 증명이 아니다.

**따라서 M2 잔여 계약을 채우는 것은 지금 순서가 아니다.**

---

## 2. ⏭ 다음 작업 — 싼 검증 먼저 (daria가 직접)

daria가 **실제로 관심 있는 종목 1~2곳**을 지금 CLI로 돌려보고 판단한다:

```bash
cd C:\Users\user\Desktop\why-missing
.venv\Scripts\python.exe -m adjudicator.cli <티커> finite_lived_intangibles_net
.venv\Scripts\python.exe -m adjudicator.cli <티커> operating_lease_liability
```

**판단할 것 하나**: 이 출력이 실사에 **진짜 쓸모 있나**, 아니면 흥미로운 장난감인가.

| 결과 | 다음 |
|---|---|
| 쓸모 있다 | 이게 새 제품 개념의 **TRL 3(개념 실증)**이다. 그제서야 M2 잔여·파일럿 설계로 |
| 애매/별로 | 제품 방향 재개방. 분류 엔진은 자산으로 남으니 폐기 아님 |

**세션이 할 일**: 사용 결과를 듣고 기록하기. **먼저 코드를 더 쓰자고 제안하지 말 것.**

---

## 3. 절대 무르면 안 되는 계약 (전부 테스트로 고정됨)

| # | 계약 | 왜 |
|---|---|---|
| 1 | **"누락 확정"은 출력 불가**(M4) | 탐지력 검증 불가 확정(스파이크5 X1 미달). 후보는 영원히 사람에게 넘기는 질문 |
| 2 | **후보 판정엔 사각 경고 필수** | companyfacts는 차원 태그·본문 기재를 못 본다. 골든의 Sturm Ruger가 산 증거 |
| 3 | **companyfacts는 accession 필터로만** | 무필터는 정정 병합본(스파이크6 금지). 무필터 경로 부존재를 테스트가 고정 |
| 4 | **흐름 개념 입구 거부** | 정당한 0을 못 가른다(스파이크2가 개념 하나를 죽인 이유) |
| 5 | **404 ≠ 조회 실패** | 스파이크6 1차의 실수 — 접근 실패를 부재로 기록 |
| 6 | **peer 셀 n≥10, 미달 시 판정 불가** | 억지 셀 금지. 4곳은 peer가 아니라 소문 |
| 7 | **큰 회사가 상위권이면 나를 먼저 의심** | 실전 **4연속 적중** (M1이 4번째) |
| 8 | **사전등록 기준은 결과 보고 안 바꾼다** | 근소 미달 반올림 금지(C′ 기각의 근거) |

---

## 4. 저장소 지도

```
adjudicator/     concepts · identity · verdict · peers · freshness · sec · live · cli · safewrite · sanity
tests/           goldens.py(단일 출처) · test_contracts · test_golden_spike4 · test_cli   [53 passed]
scripts/         reproduce_spike4.py(M1 재현) · spike5_* · spike6_*(레거시)
docs/            THESIS · FINDINGS · mvp-brief · m1-golden-reproduction · m3-cli · spikes/ · data/
```

- **FINDINGS.md** = 최종 산출물(6형태 분류 체계). 인용 시 **탐지력 검증 불가 단서 필수**.
- **THESIS.md** 7절 = 결정 대장, 9절 = 마감 선언.
- 리허설 저장소: https://github.com/dariajkim-star/crm_targeting_lab (동결, 401 tests)

---

## 5. 파티

BMAD 파티는 **crm 저장소에 설치**돼 있다(`_bmad`). 이 폴더엔 없으므로, 파티가 필요하면
멤버를 인라인으로 소환하면 된다 — 로스터: Winston(아키텍트) · Mary(분석) · Amelia(개발) ·
John(PM) · Paige(문서) · Sally(UX) · **레아**(포렌식 회계, 스크리너 단골) · **그럼발**(적대적) ·
Boundary(엣지케이스).

파티 기억(전 세션 기록)은 crm 저장소 `docs/party-mode/memories/installed/.memlog.md`에 있다.
맥락이 필요하면 그 파일을 읽을 것. 새 파티 기록은 이 저장소에 새로 시작.

---

## 6. 열린 잔무

- [ ] **계보 계약 정식 명칭** — jargon 확인 후 daria 확정 (현재 가칭)
- [ ] M2 잔여: 계약 4·5·7은 해당 계층 미도입이라 휴면 (브리프대로 — 서두르지 말 것)
- [ ] peer 맥락을 CLI에 연결 (캐시된 분기 필요) — 사용 검증 후 판단
- [ ] 파티 로스터 스크립트 cp949 인코딩 크래시 (crm 저장소 도구 문제)
